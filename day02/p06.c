#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
	//any nonzero value is treated as true
	//if(100) //if(-1)
	int x = 0;
	//if(scanf("%d", &x))
	if(printf(""))
	{
		fprintf(stdout, "if");
	}
	else
	{
		fprintf(stdout, "else");
	}

	//exit main
	exit(0);
}

